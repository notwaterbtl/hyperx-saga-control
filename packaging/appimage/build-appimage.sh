#!/usr/bin/env bash
set -euo pipefail

cat <<'MSG'
AppImage build helper placeholder.

The current recommended universal Linux distribution method is:
  ./install.sh

An AppImage can be built later with linuxdeploy/linuxdeploy-plugin-qt, but HID
access will still require host-side udev permissions.
MSG
